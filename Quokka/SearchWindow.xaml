<!--I am new to WPF & C# - any help or advice is welcome-->

<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Quokka"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:tb="http://www.hardcodet.net/taskbar" 
        x:Class="Quokka.SearchWindow"
        mc:Ignorable="d"
        Title="Quokka"
        Topmost="True"
        AllowsTransparency="True"
        WindowState="Maximized"
        ResizeMode="NoResize"
        WindowStyle="None"
        ShowInTaskbar="False"
        Background="Transparent"
        WindowStartupLocation="CenterScreen">

    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">

        <Grid.RowDefinitions>
            <RowDefinition Height="5*" />
            <RowDefinition Height="5*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="1">

            <Border
                    x:Name="EntryField"
                    BorderBrush="#FF000000"
                    BorderThickness="1"
                    CornerRadius="32"
                    Background="White"
                    Height="64"
                    Width="720"
                    VerticalAlignment="Top"
                    >

                <Grid>

                    <Grid Grid.Row="1">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="656*"/>
                            <ColumnDefinition Width="64*"/>
                        </Grid.ColumnDefinitions>

                        <Image x:Name="SearchIcon" Width="64" Source="Resources/SearchIcon.png" Grid.Column="1"/>

                        <ui:TextBox
                            PlaceholderText="Enter Query"
                            Height="50"
                            MinWidth="720"
                                Width="720"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Left"
                                x:Name="SearchTermTextBox"
                                Margin="10"
                                Cursor="IBeam"
                                FontFamily="Cascadia Code"
                                MaxLines="1"
                                AutoWordSelection="True"
                                Background="Transparent"
                                VerticalContentAlignment="Center"
                                SpellCheck.IsEnabled="True"
                                Language="en-US"
                                TextChanged="onQueryChange"
                                FontSize="24"
                                BorderBrush="Transparent"
                                BorderThickness="0"
                            >
                        </ui:TextBox>

                    </Grid>

                </Grid>

            </Border>

        </Grid>

        <Grid Grid.Row="2" Margin="0,74,0,0" VerticalAlignment="Top" >

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="720*"/>
            </Grid.ColumnDefinitions>

            <Border
                x:Name="ResultsBox"
                BorderBrush="#FF000000"
                BorderThickness="1"
                CornerRadius="32"
                Background="White"
                MinHeight="64"
                Height="auto"
                Width="720"
                >

                <TextBlock
                x:Name="ResultsText"
                IsHitTestVisible="False"
                Text="Typed:"
                VerticalAlignment="Center"
                HorizontalAlignment="Left"
                Margin="10,0,0,0"
                Foreground="Black"
                FontSize="24"
                Background="Transparent"
                FontFamily="Cascadia Code"
                TextWrapping="Wrap"
                Height="auto"
                Width="710"
                />

            </Border>

        </Grid>

    </Grid>

</Window>
